<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>アーティスト一覧</title>
</head>
<body>
    <h1>登録アーティスト一覧</h1>
    
    <p><a href="/artists/create">＋ アーティストを追加する</a></p>

    <table border="1">
        <thead>
            <tr>
                <th>ID</th>
                <th>アーティスト名 (Name)</th>
                <th>ジャンル (Genre)</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach((item) => { %>
            <tr>
                <td><%= item.id %></td>
                <td><%= item.name %></td>
                <td><%= item.genre %></td>
                <td>
                    <a href="/artists/<%= item.id %>">詳細</a> | 
                    <a href="/artists/edit/<%= item.id %>">編集</a> | 
                    <a href="/artists/delete/<%= item.id %>">削除</a>
                </td>
            </tr>
            <% }); %>
        </tbody>
    </table>

    <hr>
    <a href="/">トップに戻る</a>
</body>
</html>
