<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>場所の編集</title>
</head>
<body>
    <h1>場所情報の編集</h1>

    <form action="/places/update/<%= data.id %>" method="post">
        <div>
            <label for="name">場所名:</label>
            <input type="text" id="name" name="name" value="<%= data.name %>" required>
        </div>
        <div>
            <label for="vibe">雰囲気:</label>
            <input type="text" id="vibe" name="vibe" value="<%= data.vibe %>" required>
        </div>
        <div>
            <label for="description">説明:</label>
            <textarea id="description" name="description" rows="4" cols="50"><%= data.description %></textarea>
        </div>
        
        <br>
        <button type="submit">更新する</button>
    </form>

    <br>
    <a href="/places">一覧に戻る</a>
</body>
</html>
